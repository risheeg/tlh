[project]
name = "vault-ingest"
version = "0.1.0"
description = "Cloudflare Python Worker for personal document vault ingestion"
requires-python = ">=3.12"
dependencies = [
    "workers-py>=1.90",
    "jsonschema",
    "json-repair",
]

[dependency-groups]
dev = [
    "workers-runtime-sdk",
]
